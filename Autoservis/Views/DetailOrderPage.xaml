<Page x:Class="Autoservis.Views.DetailOrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="Detail Zakázky" Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="10">
            <TextBlock Text="Detail Zakázky" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
            <Button Click="BtnClose_Click" Content="X" Width="40" Height="40" HorizontalAlignment="Right" Background="#FF5555" Foreground="White" FontSize="18" FontWeight="Bold"/>
        </Grid>

        <TabControl Grid.Row="1" Margin="10">

            <TabItem Header="Informace">
                <StackPanel Margin="10">
                    <TextBlock Text="Název zakázky:" FontWeight="Bold"/>
                    <TextBox x:Name="OrderNameBox" Margin="0,0,0,10"/>

                    <TextBlock Text="Datum:" FontWeight="Bold"/>
                    <DatePicker x:Name="OrderDatePicker" Margin="0,0,0,10"/>

                    <TextBlock Text="Stav:" FontWeight="Bold"/>
                    <ComboBox x:Name="OrderStateComboBox" Margin="0,0,0,10"/>

                    <TextBlock Text="Zákazník:" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock x:Name="CustomerNameText" FontSize="14"/>

                    <TextBlock Text="Auto:" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock x:Name="CarNameText" FontSize="14"/>

                    <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                        <TextBlock Text="Celková cena:" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TotalPriceText" Text="0 Kč" FontSize="18" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="ForestGreen"/>
                    </StackPanel>
                </StackPanel>
            </TabItem>

            <TabItem Header="Materiál">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Content="Přidat materiál" Click="AddMaterial_Click" Width="120" HorizontalAlignment="Left" Margin="5"/>
                    <DataGrid x:Name="MaterialsGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Název" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Kusů" Binding="{Binding Quantity}"/>
                            <DataGridTextColumn Header="Cena/ks" Binding="{Binding Price}"/>
                            <DataGridTextColumn Header="Celkem" Binding="{Binding TotalPrice}"/>
                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Smazat" Click="DeleteMaterial_Click" Background="Red" Foreground="White"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Práce">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Content="Přidat práci" Click="AddWork_Click" Width="120" HorizontalAlignment="Left" Margin="5"/>
                    <DataGrid x:Name="WorksGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Hodiny" Binding="{Binding Hours}" Width="*"/>
                            <DataGridTextColumn Header="Sazba" Binding="{Binding Price}"/>
                            <DataGridTextColumn Header="Celkem" Binding="{Binding TotalPrice}"/>
                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Smazat" Click="DeleteWork_Click" Background="Red" Foreground="White"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Fotodokumentace">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Content="Nahrát fotku" Click="AddPhoto_Click" Width="120" HorizontalAlignment="Left" Margin="5"/>

                    <ListBox x:Name="PhotosList" Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="5">
                                    <Image Source="{Binding FilePath}" Width="150" Height="100" Stretch="UniformToFill"/>
                                    <Button Content="Smazat" Click="DeletePhoto_Click" Margin="0,5,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Smazat celou zakázku" Click="DeleteOrder_Click" Background="DarkRed" Foreground="White" Margin="0,0,10,0" Width="150" Height="40"/>
            <Button Content="Uložit změny" Click="SaveChanges_Click" Background="ForestGreen" Foreground="White" Width="150" Height="40"/>
        </StackPanel>
    </Grid>
</Page>