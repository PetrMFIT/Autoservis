<Page x:Class="Autoservis.Views.CreateOrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      mc:Ignorable="d" 
      d:DesignHeight="900" d:DesignWidth="1100"
      Title="Nová zakázka"
      Background="White"
      FontFamily="Segoe UI">

    <Page.Resources>
        <Style x:Key="MainComboBox" TargetType="ComboBox">
            <Setter Property="Padding" Value="10,0,0,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#999999"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Grid.Column="2" Focusable="false" ClickMode="Press" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="25"/>
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M0,0 L8,0 L4,4 z" Fill="#555" Stretch="Fill" Width="10" Height="6"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="250">
                                    <Border x:Name="DropDownBorder" Background="White" BorderThickness="1" BorderBrush="#999" Margin="0,2,0,0">
                                        <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#333333"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                <Setter Property="Foreground" Value="#555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GreenButton" TargetType="Button">
            <Setter Property="Background" Value="#4CD964"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5CE074"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GreyButton" TargetType="Button">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D0D0D0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#999"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="8,5">
                            <StackPanel Orientation="Horizontal">
                                <icon:PackIconMaterial Kind="Close" VerticalAlignment="Center" Width="12" Height="12" Margin="0,0,5,0" Foreground="{TemplateBinding Foreground}"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#D32F2F"/>
                    <Setter Property="Background" Value="#FFEBEE"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#444"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="ModeRadioButton" TargetType="RadioButton">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bdr" Background="Transparent" BorderBrush="#CCC" BorderThickness="1" CornerRadius="3" Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="#EEE"/>
                                <Setter TargetName="bdr" Property="BorderBrush" Value="#999"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderThickness" Value="0,0,1,2"/>
            <Setter Property="BorderBrush" Value="#A0A0A0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                <ContentPresenter VerticalAlignment="Center" SnapsToDevicePixels="True"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F2F2F2"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#D8D8D8"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="0">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="30,30,30,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Click="CloseButton_Click" Style="{StaticResource CloseButtonStyle}" Content="Zavřít" HorizontalAlignment="Left"/>
                    <TextBlock Grid.Column="1" Text="Nová zakázka" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="#333"/>
                </Grid>

                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <Label Content="NÁZEV ZAKÁZKY *"/>
                        <TextBox x:Name="OrderNameBox" Style="{StaticResource InputBox}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <Label Content="DATUM"/>
                        <DatePicker x:Name="OrderDatePicker" Height="35" VerticalContentAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Grid.Column="4">
                        <Label Content="STAV"/>
                        <ComboBox x:Name="OrderStateComboBox" Style="{StaticResource MainComboBox}"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#F9F9F9" CornerRadius="5" Padding="15" BorderBrush="#EEE" BorderThickness="1">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <icon:PackIconMaterial Kind="Account" Foreground="#666" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Zákazník" FontWeight="SemiBold" Foreground="#444"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <RadioButton x:Name="RadioCustExisting" Content="Vybrat existujícího" IsChecked="True" Checked="RadioCust_Checked" Style="{StaticResource ModeRadioButton}"/>
                                <RadioButton x:Name="RadioCustNew" Content="Vytvořit nového" Checked="RadioCust_Checked" Style="{StaticResource ModeRadioButton}"/>
                            </StackPanel>

                            <Grid x:Name="CustomerSearchPanel">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="CustomerSearchBox" Style="{StaticResource InputBox}" KeyDown="CustomerSearchBox_KeyDown" Tag="Jméno, telefon..."/>
                                <Button Grid.Column="1" Click="CustomerSearchButton_Click" Content="Hledat" Width="70" Margin="10,0,0,0" Style="{StaticResource GreyButton}" Height="35" Padding="0"/>

                                <Popup x:Name="CustomerPopup" PlacementTarget="{Binding ElementName=CustomerSearchBox}" StaysOpen="False" Width="300">
                                    <Border Background="White" BorderBrush="#999" BorderThickness="1">
                                        <ListBox x:Name="SearchedCustomers" MaxHeight="150" MouseDoubleClick="SearchedCustomer_MouseDoubleClick">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="5"/>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </Border>
                                </Popup>
                            </Grid>

                            <Grid x:Name="SelectedCustomerInfo" Visibility="Collapsed">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock x:Name="CustNameTxt" FontWeight="Bold" FontSize="16" Foreground="#333"/>
                                    <TextBlock x:Name="CustPhoneTxt" Foreground="#666" Margin="0,2,0,0"/>
                                    <TextBlock x:Name="CustEmailTxt" Foreground="#666"/>
                                </StackPanel>
                                <Button Grid.Column="1" Click="ClearSelectedCustomerButton_Click" VerticalAlignment="Top" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                    <icon:PackIconMaterial Kind="Close" Foreground="Red"/>
                                </Button>
                            </Grid>

                            <Grid x:Name="NewCustomerForm" Visibility="Collapsed">
                                <StackPanel>
                                    <Label Content="Jméno *"/>
                                    <TextBox x:Name="NewCustName" Style="{StaticResource InputBox}"/>
                                    <Label Content="Telefon"/>
                                    <TextBox x:Name="NewCustPhone" Style="{StaticResource InputBox}"/>
                                    <Label Content="Email"/>
                                    <TextBox x:Name="NewCustEmail" Style="{StaticResource InputBox}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Background="#F9F9F9" CornerRadius="5" Padding="15" BorderBrush="#EEE" BorderThickness="1">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <icon:PackIconMaterial Kind="Car" Foreground="#666" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Vozidlo" FontWeight="SemiBold" Foreground="#444"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <RadioButton x:Name="RadioCarExisting" Content="Vybrat existující" IsChecked="True" Checked="RadioCar_Checked" Style="{StaticResource ModeRadioButton}"/>
                                <RadioButton x:Name="RadioCarNew" Content="Vytvořit nové" Checked="RadioCar_Checked" Style="{StaticResource ModeRadioButton}"/>
                            </StackPanel>

                            <Grid x:Name="CarSearchPanel">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="CarSearchBox" Style="{StaticResource InputBox}" KeyDown="CarSearchBox_KeyDown" GotFocus="CarSearchBox_GotFocus" Tag="SPZ, Model..."/>
                                <Button Grid.Column="1" Click="CarSearchButton_Click" Content="Hledat" Width="70" Margin="10,0,0,0" Style="{StaticResource GreyButton}" Height="35" Padding="0"/>

                                <Popup x:Name="CarPopup" PlacementTarget="{Binding ElementName=CarSearchBox}" StaysOpen="False" Width="300">
                                    <Border Background="White" BorderBrush="#999" BorderThickness="1">
                                        <ListBox x:Name="SearchedCars" MaxHeight="150" MouseDoubleClick="SearchedCar_MouseDoubleClick">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding BrandModel}" FontWeight="Bold"/>
                                                        <TextBlock Text="{Binding SPZ}" Foreground="Gray" FontSize="11"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </Border>
                                </Popup>
                            </Grid>

                            <Grid x:Name="SelectedCarInfo" Visibility="Collapsed">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock x:Name="CarModelTxt" FontWeight="Bold" FontSize="16" Foreground="#333"/>
                                    <TextBlock x:Name="CarSPZTxt" Foreground="#666" FontWeight="SemiBold" Margin="0,2,0,0"/>
                                    <TextBlock x:Name="CarVINTxt" Foreground="#555"/>
                                </StackPanel>
                                <Button Grid.Column="1" Click="ClearSelectedCarButton_Click" VerticalAlignment="Top" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                    <icon:PackIconMaterial Kind="Close" Foreground="Red"/>
                                </Button>
                            </Grid>

                            <Grid x:Name="NewCarForm" Visibility="Collapsed">
                                <StackPanel>
                                    <Label Content="Značka a Model *"/>
                                    <TextBox x:Name="NewCarModel" Style="{StaticResource InputBox}"/>
                                    <Label Content="SPZ *"/>
                                    <TextBox x:Name="NewCarSPZ" Style="{StaticResource InputBox}"/>
                                    <Label Content="Rok výroby"/>
                                    <ComboBox x:Name="NewCarYear" Style="{StaticResource MainComboBox}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid Grid.Row="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border BorderBrush="#EEE" BorderThickness="0,0,0,1" Margin="0,0,0,5">
                        <Grid>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <icon:PackIconMaterial Kind="Cart" Foreground="#666" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Materiál" Style="{StaticResource SectionHeader}"/>
                            </StackPanel>
                            <Button Click="AddMaterialButton_Click" Content="+ Přidat" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Foreground="#4CD964" FontWeight="Bold" Cursor="Hand"/>
                        </Grid>
                    </Border>
                    <DataGrid x:Name="OrderMaterialDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Height="150"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE" Background="White" BorderThickness="0" RowHeight="35">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Název" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Počet" Binding="{Binding Quantity}" Width="50"/>
                            <DataGridTextColumn Header="Cena" Binding="{Binding TotalPrice}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <Grid Grid.Row="6">
                    <Border BorderBrush="#EEE" BorderThickness="0,0,0,1" Margin="0,0,0,5">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <icon:PackIconMaterial Kind="Wrench" Foreground="#666" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Práce" Style="{StaticResource SectionHeader}"/>
                            </StackPanel>
                            <Button Click="AddWorkButton_Click" Content="+ Přidat" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Foreground="#4CD964" FontWeight="Bold" Cursor="Hand"/>
                        </Grid>
                    </Border>
                    <DataGrid x:Name="OrderWorkDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Height="150"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE" Background="White" BorderThickness="0" RowHeight="35">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Popis" Binding="{Binding Description}" Width="*"/>
                            <DataGridTextColumn Header="Hodiny" Binding="{Binding Hours}" Width="50"/>
                            <DataGridTextColumn Header="Cena" Binding="{Binding TotalPrice}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <StackPanel Grid.Row="8" HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock Text="CELKEM:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock x:Name="TotalPriceFillBlock" Text="0 Kč" FontSize="20" FontWeight="Bold" Foreground="#4CD964"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <Border Grid.Row="1" Background="White" BorderBrush="#EEEEEE" BorderThickness="0,1,0,0" Padding="20">
            <Button Click="AddOrderButton_Click" Style="{StaticResource GreenButton}" Height="45" Content="VYTVOŘIT ZAKÁZKU"/>
        </Border>
    </Grid>
</Page>