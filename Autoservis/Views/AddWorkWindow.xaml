<Window x:Class="Autoservis.Views.AddWorkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Autoservis.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Title="AddWorkWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid x:Name="WorkInfo" Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" HorizontalAlignment="Center">Vytvoreni Prace</TextBlock>
            <StackPanel x:Name="WorkInfoBoxes">
                <TextBlock x:Name="WorkHoursBlock">Hodiny:</TextBlock>
                <TextBox x:Name="WorkHoursBox"></TextBox>

                <TextBlock x:Name="WorkPriceBlock">Cena:</TextBlock>
                <TextBox x:Name="WorkPriceBox"></TextBox>
            </StackPanel>
        </Grid>
        <Button x:Name="AddWorkButton" Grid.Row="1" Click="AddWorkButton_Click">Pridat</Button>
    </Grid>
</Window>
